import { ShowNavDirective } from './show-nav.directive';

describe('ShowNavDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowNavDirective();
    expect(directive).toBeTruthy();
  });
});
